import { ImageSourcePropType } from "react-native";

import link_card from "@/app_assets/images/link_card.png";

type Props = {
	link_card_image: ImageSourcePropType;
};

export const appImages = {
	link_card,
};
